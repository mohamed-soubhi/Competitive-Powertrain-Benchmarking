"""LUZA-Horse shared core.

Small, dependency-light building blocks reused across the mining, analysis, and
ML scripts so business logic (paths, config, data IO, plot theme, schema
validation) lives in exactly one place.
"""

__version__ = "0.2.0"
